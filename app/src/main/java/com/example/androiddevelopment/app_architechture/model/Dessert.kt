package com.example.androiddevelopment.app_architechture.model

class Dessert(cImageId: Int, cPrice: Int, cStartProdAmount: Int) {
    val imageId: Int = cImageId
    val price: Int = cPrice
    val startProdAmount: Int = cStartProdAmount
}